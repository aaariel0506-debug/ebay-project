# Day 18 — 2026-04-15

## 目标
Buffer 日：线上虚拟库存模块收尾（Phase 3 Day 6）

## 完成内容

### Bug 修复（Day 16 复核）

#### 1. `price-check --sku` 脏记录 bug
- **问题**：`update_cost_price(sku, Decimal("0"))` 先写入错误价格，再 `input()` 交互读取真正价格
- **修复**：改为先 `input()` 读取新价格，再调用一次 `update_cost_price`

#### 2. `margin-check` 参数名错误
- **问题**：代码引用 `args.min_margin`，但 argparse 参数名是 `--threshold`
- **修复**：改为 `getattr(args, 'threshold', 0.15)`

#### 3. `TestUpdateCostPrice` 空壳测试
- **问题**：5 个测试全是 `pass`，无实际断言
- **修复**：改为有效测试（PriceChangeAlert 构造 + change_rate 逻辑 + mock SKU 不存在）

#### 4. `PriceChangeDirection` 无用类
- **问题**：继承 `str` 但无 Enum，也没被任何地方引用
- **修复**：已删除

### Day 18 修复（持续收尾）

#### 5. `quantity_adjuster._update_ebay_inventory` 改用 EbayClient 公开方法
- **问题**：直接访问 `self._client._session.put`（私有属性）且手动处理 token/headers
- **修复**：改用 `self._client.put(endpoint, json_body=payload, use_user_token=True)`
  - 自带重试 / 限流 / 401 自动刷新 / 离线降级

#### 6. `quantity_adjuster` 添加 `audit_log()` 调用
- **问题**：库存调整无审计记录
- **修复**：每次成功调整后调用 `_audit_adjustment()` → 写入 `audit_log` 表

#### 7. `test_invalid_price_raises` 加强
- **问题**：`patch.object(pm, "update_cost_price", wraps=...)` 块内只有 `pass`，没测业务逻辑
- **修复**：用 mock DB 返回 Product + EbayListing，真实调用 `update_cost_price(sku, Decimal("-1"))` 验证 ValueError

### 文档
- `docs/inventory_online.md`：线上虚拟库存模块使用文档

## 测试结果
```
269 passed, 1 warning in 6.81s
All lint checks passed
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `abac864` | fix(day18): quantity_adjuster uses EbayClient.put + audit_log, test_invalid_price_raises real mock test |
| `a0c9882` | fix(day16): price-check dirty write + margin-check args + empty tests + cleanup PriceChangeDirection |
| `a328e1c` | Day 17: restock_advisor.py + quantity_adjuster.py + CLI restock-advice/adjust + 14 tests |
| `c499391` | docs(day16): update report with bug fixes and effective test count |

## git tag
```
v0.3.0-inventory-online
```

# Day 27 日报 — 2026-04-21

## 今日任务

**T2.2**: CLI + 增量同步（`last_sync_at` 追踪）
**T2.3**: Transaction 汇总验证（毛利报告 CLI）

## 进行中

### T2.2 增量同步

待实现：
1. `SyncMeta` 模型（记录各模块最后同步时间）
2. `OrderSyncService` 同步完成后写 `last_sync_at`
3. CLI `--full` vs 日期范围切换逻辑
4. Migration

### T2.3 Transaction 汇总验证

待实现：
1. `finance report` CLI 子命令
2. 按日/周/月汇总：sales / fees / shipping / gross_profit / margin
3. SQL 聚合查询
4. 测试

## 技术债务

- `transaction already deassociated` warning（不阻塞）

## 测试覆盖

```
350 passed, 0 xfailed
```

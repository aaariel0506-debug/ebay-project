# Day 9 — 2026-04-15（续2）

## 目标
Listing 模板系统（Day 9 主目标 + 修复确认）

## 修复内容

### Fix 1 — apply_template 硬编码 currency/marketplace_id
- `ListingTemplate` 新增 `currency`（默认 `"USD"`）和 `marketplace_id`（默认 `"EBAY_US"`）字段
- `apply_template()` 现在读模板字段，不再硬编码
- Migration: `e617403b1604_add_currency_marketplace_to_listing_templates`

### Fix 2 — from_existing API 路径错误
- **原问题**：`ebay_item_id` → `/sell/inventory/v1/offer/{ebay_item_id}`（Offer ID ≠ Item ID）
- **新逻辑**：双路径
  - `sku` → `GET /sell/inventory/v1/inventory_item/{sku}`（优先，返回完整 policies）
  - `item_id` → `GET /buy/browse/v1/item/{item_id}`（fallback，仅公开信息）
- 参数改为 `sku` + `item_id` 分离

## 测试结果
```
149 passed, 1 SAWarning
Day 9 tests: 24
Fix verification: pass
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `eab8163` | fix(template): currency/marketplace_id from template; from_existing dual-path (sku→InventoryAPI, item_id→BrowseAPI) |

## Day 9 状态
✅ 零遗留，验收通过

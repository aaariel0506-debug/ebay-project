# Day 21 — 2026-04-16

## 目标
库存盘点 + 差异调整（Phase 4 Day 3）

## 完成内容

### `core/models/stocktake.py` — 新建
- `Stocktake` 模型：盘点单主表（status / started_at / finished_at / operator）
- `StocktakeItem` 模型：盘点明细（sku / system_quantity / actual_quantity / difference）
- `StocktakeStatus` enum：in_progress / finished / cancelled

### `alembic/versions/27e8b3f6c1d0_create_stocktake_tables.py` — 新建

### `modules/inventory_offline/stocktake_service.py` — 新建
- `start_stocktake(skus, operator)`：创建盘点单，锁定所有 SKU 的系统库存快照
- `record_count(stocktake_id, counts)`：录入实际清点数量，自动计算差异
- `finish_stocktake(stocktake_id)`：结束盘点，生成 `Inventory(type=ADJUST)` 记录，发布 `STOCKTAKE_FINISHED` 事件
- `cancel_stocktake(stocktake_id)`：取消盘点（仅 in_progress 可取消）
- `get_stocktake(stocktake_id)`：获取完整盘点信息含差异汇总
- `list_stocktakes(status, limit)`：列出盘点单

### `tests/test_stocktake_service.py` — 新建（11 tests）
- 覆盖：创建盘点 / 录入清点 / 结束盘点 / 取消 / 差异计算 / 无差异情况 / 已结束不可重复操作

### `api/cli/inventory_offline_cli.py` — 扩展
- `stocktake-start` / `stocktake-record` / `stocktake-finish` / `stocktake-list`

### `main.py` — 扩展
- 补注册 stocktake 命令到 argparse

## 测试结果
```
298 passed, 8 warnings in 9.10s
Day 21 新增测试：11 tests（total 298）
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `667de48` | Day21b: stocktake CLI commands + main.py registration |
| `5b4f553` | Day 21: stocktake models + service + 11 tests |

## Bug 修复（Day 21 复核）

### `finish_stocktake` 后 `get_stock` 验证缺失
- **问题**：测试验证了 `adjustment_records == 1` 和 `total_difference == -2`，但没有验证 `get_stock()` 实际返回调整后的库存
- **修复**：在 `test_finish_stocktake_creates_adjust_records` 中加 `get_stock` 断言，确认库存从 10 变为 8

## 小问题（暂不处理）
- `STOCKTAKE_FINISHED` 事件类型不在 Day 5 初始事件清单中（清单有 `STOCK_ADJUST`），命名风格不一致。不影响功能，Day 24 可统一。

## Day 22 计划
- 线上线下库存联动
- 监听 `STOCK_OUT` 事件 → 自动调用 eBay Inventory API 扣减数量
- 重复出库去重（`related_order` 防重复扣减）
- `modules/inventory_online/event_handlers.py`

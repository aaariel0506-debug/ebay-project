# Day 16 — 2026-04-15

## 目标
供应价格变化检测 + 阈值警告（Phase 3 Day 4）

## 完成内容

### `core/models/price_history.py` — 新建
- `SupplierPriceHistory` 模型：供应商价格历史记录表
- 字段：sku / supplier / price / currency / recorded_at / note
- 索引：sku、recorded_at、sku+recorded_at 复合索引
- 与 Product 表的外键关联（cascade delete）

### `modules/inventory_online/price_monitor.py` — 新建
- `PriceChangeAlert` dataclass：价格变化预警信息
- `BatchPriceUpdateResult` dataclass：批量更新结果
- `PriceMonitor.update_cost_price(sku, new_price)`：
  - 将旧价格记录到 SupplierPriceHistory 表
  - 更新 Product.cost_price
  - 变化超过阈值（默认 10%）→ 发布 PRICE_ALERT 事件
  - 计算新利润率，低于阈值（默认 15%）→ 建议调整售价
- `PriceMonitor.batch_update_from_csv(csv_path)`：批量价格更新
- `PriceMonitor.get_price_history(sku)`：查询价格变动历史
- `PriceMonitor.get_latest_price(sku)`：获取当前进货价

### `ebay-ms/api/cli/inventory_online_cli.py` — 新建
- `price-check`：检查进货价变化（单 SKU 或 CSV 批量）
- `price-history`：查看 SKU 进货价历史
- `margin-check`：检查利润率低于阈值的商品

### `main.py` — 扩展
- 添加 `inventory online` 子命令（status / alert / sync / price-check / price-history / margin-check）

### `core/models/__init__.py` — 更新
- 导出 `SupplierPriceHistory`
- 添加 Product → SupplierPriceHistory 的 relationship

### `alembic` migration — 新建
- `102cfcaae272_create_supplier_price_history.py`

### `tests/conftest.py` — 修复
- `setup_database` fixture：从 `"head"` 改为 `"102cfcaae272"`（解决多头冲突）

### `tests/test_price_monitor.py` — 新建（18 tests，**全部为有效测试**）
- `TestUpdateCostPrice`（5 tests）：change_rate 触发逻辑、PriceChangeAlert 构造
- `TestPriceChangeRate`（4 tests）：变化率计算
- `TestMarginCalculation`（2 tests）：利润率计算
- `TestBatchUpdate`（1 test）：批量结果计数
- `TestPriceChangeAlert`（1 test）：Alert dataclass
- `TestPriceMonitorInit`（2 tests）：阈值初始化
- `TestPriceHistoryModel`（2 tests）：模型字段验证
- `TestPriceAlertEventPayload`（1 test）：事件 payload 结构

## 测试结果
```
269 passed, 1 warning in 9.03s
Day 16 新增有效测试：18 tests（total 269）
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `76d440c` | Day 16: price_monitor.py + price_history model + inventory online CLI + 18 tests (total 255) |
| `6bbfdca` | fix: repair alembic migration chain + conftest head + lint cleanup |
| `a0c9882` | fix(day16): price-check dirty write + margin-check args + empty tests + cleanup PriceChangeDirection |

## Bug 修复（Day 16 复核后）
| 问题 | 修复 |
|------|------|
| `price-check --sku` 先写 `Decimal("0")` 到 DB 再 input（脏记录） | 改为先 `input()` 交互读取，再调用 `update_cost_price` |
| `margin-check` 引用 `args.min_margin`（参数名是 `--threshold`） | 改为 `getattr(args, 'threshold', 0.15)` |
| `TestUpdateCostPrice` 5 个空壳 `pass` 测试 | 改为有效测试（PriceChangeAlert 构造 + change_rate 逻辑 + mock SKU 不存在） |
| `PriceChangeDirection` 类无用且无 Enum | 已删除 |

## Day 17 计划
- 自动补货建议 + eBay 库存调整接口
- `modules/inventory_online/restock_advisor.py`：补货建议逻辑
- `modules/inventory_online/quantity_adjuster.py`：eBay 库存调整接口
- CLI：`python main.py inventory online restock-advice / adjust`

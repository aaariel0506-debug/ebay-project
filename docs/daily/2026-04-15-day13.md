# Day 13 — 2026-04-15

## 目标
Day 13：拉取 eBay Active Listings 同步到本地（Phase 3 Day 1）

## 完成内容

### `modules/inventory_online/__init__.py` — 新建
### `modules/inventory_online/sync_service.py` — 新建
- `SyncResult` dataclass：同步统计报告
- `SyncService.full_sync()`：
  - 分页拉取 `GET /sell/inventory/v1/inventory_item`（每页 100 条）
  - Upsert 每条到 `EbayListing` 表（按 sku 匹配）
  - 标记已下架 listing（本地有但 eBay 不再有 → status=ENDED）
  - API 错误处理 + 记录到 result.errors
- `_fetch_all_inventory_items()`：分页获取
- `_upsert_listing()`：创建或更新
- `_mark_ended_listings()`：标记下架

### `tests/test_sync.py` — 新建（13 tests）
- `TestSyncResult`（1 test）
- `TestFetchAll`（4 tests）：空响应、单页、多页（150 条）
- `TestFullSync`（4 tests）：新建、更新、标记 ended、API 错误处理
- `TestUpsert`（2 tests）：新建/更新路径
- `TestMarkEnded`（2 tests）：ended 计数正确

## 测试结果
```
205 passed, 1 SAWarning（已知）
新增 Day 13：13 tests
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `7a583a3` | Day 13: SyncService.full_sync() + 13 tests (total 205) |

## Day 14 计划
- `modules/inventory_online/monitor.py`：库存监控
  - `list_all()`：列出所有商品库存
  - `list_out_of_stock()`：缺货商品
  - `list_low_stock(threshold)`：低库存商品
- CLI 命令：`python main.py inventory online status`
- 同步后自动 STOCK_ALERT 事件发布

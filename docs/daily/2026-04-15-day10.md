# Day 10 — 2026-04-15

## 目标
批量上新 + CSV 导入（按 35 天计划 Day 10）

## 完成内容

### modules/listing/importer.py — 新建
- `ImportRow`：CSV 单行数据结构
- `ImportResult` / `ImportError`：导入结果报告
- `ListingImporter`：
  - `import_file()`：支持 CSV 和 XLSX 文件
  - `import_rows()`：支持单品和变体分组
  - `generate_template()`：生成空白导入模板 CSV
  - 中断续传：`_load_progress()` / `_save_progress()` / `_clear_progress()`
- 变体分组支持：以 `is_parent` 或 `variant_sku` 为标志分组
- 错误处理：逐行失败不影响其他行，返回详细 `ImportError` 列表

### core/models/batch.py — 新建
- `BatchProgress`：批量导入进度表（batch_id PK, last_row, status）

### alembic/versions/4fbcdf4febca_create_batch_progress.py — 新建
- 创建 `batch_progress` 表

### tests/test_listing_batch.py — 新建（21 tests）
- `TestGenerateTemplate`（2 tests）
- `TestCSVParsing`（5 tests）：读取 / dict_to_row / 类型转换
- `TestParseHelpers`（6 tests）：图片 URL 解析 / 变体 specifics 解析
- `TestImportRow`（2 tests）
- `TestImportResult`（1 test）
- `TestMockProduct`（1 test）
- `TestBatchId`（1 test）
- `TestImportRowsMocked`（4 tests）：导入流程 / 缺失 SKU / CSV 文件 / 中断续传

## 测试结果
```
170 passed, 1 SAWarning（已知）
新增 Day 10：21 tests
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `034087a` | Day 10: ListingImporter (CSV/XLSX batch import) + BatchProgress ORM + test_listing_batch (21 tests) |

## 下一步（Day 11）
- 上新模块集成测试
- CLI 入口：`main.py listing create/list/template`
- `tests/integration/test_listing_e2e.py`

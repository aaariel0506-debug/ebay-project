# 📋 Daily Report — Day 4 | 2026-04-14

## 🎯 Goals
为 eBay API 客户端添加生产级弹性：重试、限流、缓存、离线降级

## ✅ Done
- [x] `core/ebay_api/retry.py` — @with_retry 装饰器，指数退避 + jitter，仅重试网络错误和 5xx
- [x] `core/ebay_api/rate_limiter.py` — RateLimiter，SQLite 持久化，80% 警告 / 95% 拒绝
- [x] `core/ebay_api/cache.py` — ResponseCache，LRU + TTL，GET 请求 5 分钟 TTL
- [x] `core/ebay_api/client.py` — 集成 retry + 限流 + 缓存 + offline 降级 + pending 队列
- [x] `config/ebay_rate_limits.yaml` — 各 API 每日限额配置
- [x] `tests/test_api_resilience.py` — 11 个弹性测试全通过

## 🐛 Issues
| 问题 | 原因 | 解决 |
|------|------|------|
| Day 3 Client 测试挂起 | EbayClient.__init__ 新增 SQLite/rate_limiter 等，mock 不全导致真实初始化卡住 | _make_client() 改用 __new__ + 独立 ResponseCache 实例隔离 |
| MagicMock 缓存 mock 失效 | `mock_cache.return_value.get.return_value = None` 仍返回 MagicMock（truthy），导致缓存命中而非 miss | _make_client() 使用干净 ResponseCache() 实例，绕过全局单例状态 |
| 两个 patch 文件误提交 | day3-all-changes.patch / day3-fix-merge.patch 在仓库根目录 | git rm 删除，commit |
| Lint `l` 变量名不规范 | E741 ambiguous variable name | l → limiter，其余 ruff --fix 自动修复 |

## 🔀 Project Adjustments
| 调整内容 | 原因 | 影响 |
|----------|------|------|
| — | — | — |

## 📊 Test Results
- Total: 31/31 passed
- New: 11 个（test_api_resilience.py）

## 🔜 Tomorrow
Day 5：事件总线 + event persistence（根据 35 天计划 Phase 1 后续任务）

## 📌 Notes
- 需关注 eBay API 每日限额（rate limit）实际阈值，与 ebay_rate_limits.yaml 配置对比验证
- pending 队列目前存于内存，Phase 2 需考虑持久化到 SQLite

# 📋 Daily Report — Day 29 | 2026-04-22

## 🎯 Goals
- 按 brief 落地整体财务看板（Finance Dashboard）
- 严格对齐 `docs/finance-semantics.md` 语义
- 保持范围收敛，不触碰 Day 28 核心实现文件

## ✅ Done
- [x] 将语义文档原样复制到 `ebay-ms/docs/finance-semantics.md`
- [x] 新增 `ebay-ms/modules/finance/dashboard.py`
- [x] 新增 `ebay-ms/tests/test_dashboard.py`
- [x] 修改 `ebay-ms/main.py`，新增 `finance dashboard` CLI 子命令
- [x] 实现 JPY 本位 dashboard 聚合：
  - `total_revenue_jpy`
  - `total_cost_jpy`
  - `total_fee_jpy`
  - `gross_profit_jpy`
  - `gross_margin`
  - `uncovered_transactions`
  - `coverage_ratio`
  - `avg_order_value_jpy`
  - `avg_order_margin`
  - Top SKU 分析
- [x] 保证 `ADJUSTMENT` 不参与聚合
- [x] 保证 `amount_jpy IS NULL` 不参与金额聚合，但计入未覆盖数
- [x] 保证 `gross_profit` 不依赖 `Transaction.profit` 字段求和
- [x] 删除不在 brief 清单内的过渡文件：`modules/finance/report.py`、`tests/test_finance_report.py`
- [x] 本地提交并 push 到 GitHub

## 🐛 Issues
| 问题 | 原因 | 解决 |
|------|------|------|
| 初版先做了 `report.py` 过渡实现 | 在完整看到 Day 29 brief v2 前，先按语义文档预落地了一版聚合 | 改为正式实现 `dashboard.py`，并删除过渡文件，恢复与 brief 一致 |
| `avg_order_margin` 测试样例初版不能证明“不是 sum-based” | 两张订单收入相同，导致算术平均与整体利润率恰好相等 | 调整测试数据，让订单权重不同，明确验证订单级平均语义 |
| 需要确认语义文档不能被改写 | brief 要求字节级一致 copy | 直接 copy 到 `ebay-ms/docs/finance-semantics.md`，并做 `cmp` 校验 |

## 🔀 Project Adjustments
| 调整内容 | 原因 | 影响 |
|----------|------|------|
| 最终实现从 `report.py` 收敛到 `dashboard.py` | Day 29 brief v2 已明确文件名与边界 | 最终 diff 更干净，便于 reviewer 验收 |
| CLI 改为 `--period / --from / --to` 组合 | brief 明确了 `finance dashboard` 参数约定 | 与验收脚本和后续 Day 30/31 扩展保持一致 |
| 保留“未采集项”警告块为固定输出 | 当前毛利润系统性偏高，不能粉饰 | 降低误用 dashboard 做经营决策的风险 |

## 📊 Test Results
- Day 29 专项：`21 passed in 0.14s`
- 全量：`411 passed, 12 warnings in 11.76s`
- Ruff: `All checks passed!`
- 语义文档校验：`cmp` 一致

## 🔜 Tomorrow
- 等 Day 29 reviewer 验收
- 如通过，可进入 Day 30（按月/按天分解报表）
- 如 reviewer 反馈边界问题，优先修正 CLI/格式化输出层，不动聚合语义核心

## 📌 Notes
- 本次提交：`5575350` — `feat(day29): add finance dashboard`
- 已 push：`origin/main`
- Day 29 最终实现严格限制在 brief 指定文件范围内

# Day 9 — 2026-04-15

## 目标
Listing 模板系统（按 35 天计划）

## 完成内容

### core/models/template.py — 新建
- `ListingTemplate` ORM：name (unique), description_template (支持占位符), category_id, condition, condition_description, shipping/return/payment_policy_id, default_price_markup, image_settings (JSON), is_default, notes
- `apply_placeholder()` 方法：替换 {title}, {brand}, {size}, {color}, {condition}, {price}
- TimestampMixin 自动 created_at/updated_at

### alembic/versions/37c8515b5725_create_listing_templates.py — 新建
- 创建 `listing_templates` 表 + 唯一索引 `ix_listing_templates_name`
- 修正 autogenerate 垃圾 DDL（DROP TABLE event_log/audit_log）

### modules/listing/template_service.py — 新建
- `create_template()`：名称唯一，is_default 自动互斥
- `list_templates()` / `get_template()` / `get_default_template()`
- `update_template()`：部分字段更新
- `delete_template()`
- `apply_template()`：模板 + Product → ListingCreateRequest（占位符自动替换）
- `from_existing()`：从 eBay 已有 listing 拉取配置存为模板（Inventory API → Browse API fallback）

### tests/test_listing_template.py — 新建（24 tests）
- `TestTemplateCreate`（5 tests）：最小创建 / 完整字段 / 唯一约束 / 默认模板互斥 / 可选字段
- `TestTemplateList`（2 tests）：空列表 / 返回所有
- `TestTemplateGet`（2 tests）：按 ID 获取 / 不存在抛异常
- `TestTemplateUpdate`（3 tests）：更新名称 / 部分字段更新 / 不存在抛异常
- `TestTemplateDelete`（2 tests）：删除存在 / 不存在抛异常
- `TestPlaceholder`（4 tests）：基本替换 / 缺失值 / None 描述 / 无匹配
- `TestApplyTemplate`（4 tests）：基本应用 / condition 覆盖 / policy IDs / 无效模板
- `TestFromExisting`（2 tests）：Inventory API / Browse API fallback

## 测试结果
```
149 passed, 1 SAWarning（已知）
新增 Day 9：24 tests
```

## 提交记录
| Commit | 内容 |
|--------|------|
| `4f4ce4f` | Day 9: ListingTemplate ORM + template_service CRUD + apply_template + from_existing (24 tests) |

## 下一步（Day 10）
- 批量上新 + CSV 导入（Day 10 计划）
- `modules/listing/importer.py` + `templates/listing_import_template.csv`

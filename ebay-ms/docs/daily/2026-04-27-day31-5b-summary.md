# Day 31.5-B Summary

## 结果
- 完成 cpass 运费导入器
- 完成 Dashboard / Breakdown 的 SHIPPING_ACTUAL 公式扩展
- 完成 finance-semantics.md v1.2 更新
- 修复 Day 31.5-A alembic roundtrip 测试副作用
- 额外最小修复 `tests/test_models.py::TestOrderCrud::test_create_order` 的历史 OrderItem 污染，否则无法达到 0 failed

## 验证结果
- ruff: All checks passed
- pytest: `497 passed, 21 warnings in 12.77s`

## diff stat
```text
ebay-ms/docs/finance-semantics.md    | 37 +++++++++++++++--------------
ebay-ms/main.py                      | 20 ++++++++++++++++
ebay-ms/modules/finance/breakdown.py | 15 +++++++-----
ebay-ms/modules/finance/dashboard.py | 37 +++++++++++++++--------------
ebay-ms/tests/test_breakdown.py      | 20 +++++++++++++++-
ebay-ms/tests/test_dashboard.py      | 45 ++++++++++++++++++++++++++++++++----
ebay-ms/tests/test_models.py         |  4 +++-
```

## 新增测试
- `tests/test_cpass_importer.py::TestParseAmount::*` — 覆盖金额字符串/None/垃圾值解析
- `tests/test_cpass_importer.py::TestStructureValidation::*` — 覆盖缺 sheet / 缺 header 的结构错误
- `tests/test_cpass_importer.py::TestMatching::*` — 覆盖匹配、未匹配、取消单、零金额、重复 tracking
- `tests/test_cpass_importer.py::TestIdempotency::test_rerun_overwrites_old_shipping_actual` — 验证 DELETE+INSERT 幂等
- `tests/test_cpass_importer.py::TestDryRun::test_dry_run_does_not_write_db` — 验证 dry-run 不写库
- `tests/test_dashboard.py::TestShippingActual::*` — 覆盖汇总字段、gross_profit、avg_order_margin、format 输出
- `tests/test_breakdown.py::TestShippingActual::*` — 覆盖 breakdown 行字段、bucket gross_profit、Ship* 列输出

## CLI smoke
### `python main.py finance import-shipping --help`
```text
usage: main.py finance import-shipping [-h] --file FILE [--dry-run]

options:
  -h, --help   show this help message and exit
  --file FILE  cpass运单费用明细.xlsx 路径
  --dry-run    模拟运行,不写 DB
```

### `python main.py finance dashboard --period this-month`
- 输出包含 `实际运费成本 : ¥0`
- 输出包含 `✅ 所有费用项已采集`

### `python main.py finance breakdown --group-by month --from 2026-01-01 --to 2026-05-01`
- 空数据时输出 `✅ 所有费用项已采集`
- 当前空表路径不显示 `Ship*` 列；有数据时测试已覆盖 `Ship*` 表头渲染

## 偏离 brief
- 1 处必要最小偏离：额外修复 `tests/test_models.py::TestOrderCrud` 的 OrderItem 清理逻辑。原因：不修无法达到 brief 要求的 `0 failed`。未改业务代码，仅补测试清理。

## 备注
- `Transaction.type` 列当前历史上仍是 `VARCHAR(10)`，`shipping_actual` 长度为 15。SQLite 本地可正常写入；PostgreSQL 上线前需要补 schema 修复。本次按 brief 不处理。
- `cpass_importer.py` 目前使用 `datetime.utcnow()`，测试通过，但存在未来弃用 warning；本次未扩 scope 处理。

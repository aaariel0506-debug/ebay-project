# 📋 Daily Report — Day 28 | 2026-04-22

## 🎯 Goals
- 完成汇率处理与多币种统一核算（JPY 本位）
- 落地 CSV 汇率导入、历史回填、JPY 写入链路
- 跑通 Day 28 测试、全量 pytest、ruff、alembic 往返

## ✅ Done
- [x] 新增 `ExchangeRate` 模型与 `exchange_rates` 表
- [x] 新增 `core/utils/currency.py`，支持精确日期命中、向前 fallback、same-currency=1.0、CSV upsert / dry_run
- [x] 将 `Transaction.amount_usd` 重命名为 `amount_jpy`
- [x] 新增 migration：`a1f3c9e7d2b4_add_exchange_rate_and_rename_amount_usd.py`
- [x] 新增历史回填脚本：`scripts/backfill_amount_jpy.py`
- [x] 改造 `OrderSyncService`，写入 `amount_jpy` / `exchange_rate`，并按 JPY 计算 `profit` / `margin`
- [x] 改造 `TransactionService`，rebuild 时重新应用汇率并按 JPY 重算
- [x] 新增 `tests/test_currency.py`
- [x] 新增 `tests/test_backfill_amount_jpy.py`
- [x] 同步更新 `tests/test_order_sync_service.py`、`tests/test_transaction_service.py`、`tests/test_models.py` 适配 Day 28 语义
- [x] 补齐 CLI：`currency import-csv`、`finance backfill-amount-jpy`
- [x] 本地提交并 push 到 GitHub

## 🐛 Issues
| 问题 | 原因 | 解决 |
|------|------|------|
| Alembic migration 在 SQLite 报 `No support for ALTER of constraints` | 初版用 `create_unique_constraint` 给 `exchange_rates` 加唯一约束 | 改为在 `create_table(...)` 内直接声明 `UniqueConstraint` |
| 旧测试大量失败 | Day 27 测试预期仍按 USD/JPY 混算语义断言 | 按 Day 28 的 JPY 本位规则统一更新旧测试预期 |
| `TransactionService` rebuild 在无汇率时抛错 | FEE/SHIPPING 写入阶段直接调用换汇，缺失汇率未兜底 | 对缺失汇率改为保留 `amount_jpy=None` / `exchange_rate=None`，不阻断 rebuild |
| CSV 非标准币种测试未拦截小写 `usd` | 初版先 `.upper()` 再校验，导致测试失真 | 改为先验证原始输入，再决定是否接受 |

## 🔀 Project Adjustments
| 调整内容 | 原因 | 影响 |
|----------|------|------|
| profit / margin 断言统一切到 JPY 本位 | Day 28 决策已确认以 JPY 为本位币 | Day 27 旧测试需要整体同步，避免误报失败 |
| `TransactionService` 对非 SALE 交易允许缺汇率落空 | 历史数据与部分订单不一定能命中汇率 | 降低回填和 rebuild 中断风险 |
| 新增 `tests/test_backfill_amount_jpy.py` 作为独立专项文件 | 降低对既有测试面的扰动 | 更容易定位 Day 28 回填问题 |

## 📊 Test Results
- Day 28 专项：`20 passed in 0.30s`
- 目标旧测试子集：`41 passed, 1 warning in 0.65s`
- 全量：`386 passed, 12 warnings in 9.03s`
- Ruff: `All checks passed!`
- Alembic: `upgrade head -> downgrade -1 -> upgrade head` 跑通

## 🔜 Tomorrow
- 等 reviewer 验收 Day 28
- 如 reviewer 有反馈，优先修正验收阻塞项
- 若 Day 28 收口，可进入下一日开发内容

## 📌 Notes
- 本次提交：`7c9b44b` — `feat(day28): add JPY-based currency normalization`
- 已 push：`origin/main`
- Day 28 已完成核心交付，当前风险已从“实现风险”降为“review 风险”
